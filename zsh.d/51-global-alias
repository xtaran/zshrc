# -*- sh -*-

# I need this so often for quick log file statistics
function sus() {
    sort "$@" | uniq -c | sort -n
}

# I need this often for sorting from the back
function rsr() {
    rev | sort "$@" | rev
}
